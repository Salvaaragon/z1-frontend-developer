import { IconProp } from '@fortawesome/fontawesome-svg-core';

export type StatusIconProps = {
  icon: IconProp;
  iconColor: string;
  text: string;
  textColor: string;
  left?: string;
  padding?: string;
  position?: string;
  top?: string;
  transform?: string;
};
